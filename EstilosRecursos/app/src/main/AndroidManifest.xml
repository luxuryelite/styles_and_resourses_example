<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="no4mat.no4mat.estilosrecursos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_pet"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_pet_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EstilosRecursos">
        <activity android:name=".MainActivity" android:theme="@style/Theme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>